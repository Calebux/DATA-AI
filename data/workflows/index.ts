import type { WorkflowTemplate } from '@/types'

export const WORKFLOW_TEMPLATES: WorkflowTemplate[] = [
  {
    id: 'weekly-saas-intel',
    name: 'Weekly SaaS Intelligence Report',
    category: 'finance_executive',
    description: 'Pull Stripe + HubSpot data, score churn risk, flag competitive threats, deliver CEO briefing by 8AM Monday.',
    icon: 'TrendingUp',
    prompt: 'Analyse MRR changes, identify at-risk accounts, and summarise competitive signals for the week.',
    triggers: ['cron'],
    outputs: ['email', 'webhook'],
    definition: {
      name: 'Weekly SaaS Intelligence Report',
      category: 'finance_executive',
      trigger: { type: 'cron', cron_expression: '0 8 * * MON', timezone: 'America/New_York' },
      steps: [
        {
          step_id: 'ingest_stripe',
          agent_role: 'data_ingestor',
          depends_on: [],
          instructions: 'Pull MRR, new/churned subscriptions, and payment failures from Stripe for the last 7 days.',
          data_sources: [{ type: 'api', connector: 'stripe' }],
          input_sources: [],
          output_keys: ['stripe_data'],
          timeout_ms: 30000,
        },
        {
          step_id: 'ingest_hubspot',
          agent_role: 'data_ingestor',
          depends_on: [],
          instructions: 'Pull deals closed, inactive contacts (>21 days), support ticket counts, and NPS scores from HubSpot.',
          data_sources: [{ type: 'api', connector: 'hubspot' }],
          input_sources: [],
          output_keys: ['hubspot_data'],
          timeout_ms: 30000,
        },
        {
          step_id: 'analyze_revenue',
          agent_role: 'analyst',
          depends_on: ['ingest_stripe'],
          instructions: 'Calculate WoW MRR change, identify primary driver, flag anomalies, write 3-sentence executive summary. Output: { summary, mrr_change_pct, primary_driver, anomalies, arr_implied, risk_level }',
          input_sources: ['stripe_data'],
          output_keys: ['revenue_analysis'],
          timeout_ms: 60000,
        },
        {
          step_id: 'analyze_churn_risk',
          agent_role: 'analyst',
          depends_on: ['ingest_stripe', 'ingest_hubspot'],
          instructions: 'Score churn probability for each flagged account. Rank top 5 by revenue impact × probability. Output: { at_risk_accounts, total_revenue_at_risk, confidence }',
          input_sources: ['stripe_data', 'hubspot_data'],
          output_keys: ['churn_analysis'],
          timeout_ms: 90000,
          consensus: { agent_count: 3, agreement_threshold: 0.67, reconciliation: 'highest_confidence' },
        },
        {
          step_id: 'synthesize',
          agent_role: 'analyst',
          depends_on: ['analyze_revenue', 'analyze_churn_risk'],
          instructions: 'Write a CEO briefing with sections: HEADLINE, REVENUE, CUSTOMERS AT RISK, RECOMMENDED ACTIONS. Direct tone, no filler.',
          input_sources: ['revenue_analysis', 'churn_analysis'],
          output_keys: ['synthesized_report'],
          timeout_ms: 60000,
        },
        {
          step_id: 'eval_report',
          agent_role: 'eval',
          depends_on: ['synthesize'],
          instructions: 'Score completeness, specificity, actionability, tone (0–1 each). Fail if any < 0.75.',
          input_sources: ['synthesized_report'],
          output_keys: ['eval_result'],
          retry_target: 'synthesize',
          max_retries: 2,
          timeout_ms: 30000,
        },
        {
          step_id: 'deliver',
          agent_role: 'delivery',
          depends_on: ['eval_report'],
          instructions: 'Format as HTML email and send to configured recipients.',
          input_sources: ['synthesized_report', 'eval_result'],
          output_keys: ['delivery_receipt'],
          timeout_ms: 15000,
        },
      ],
      output: { channels: [{ type: 'email', to: [] }, { type: 'report', format: 'pdf' }] },
    },
  },
  {
    id: 'monthly-revenue-summary',
    name: 'Monthly Revenue Summary',
    category: 'finance_executive',
    description: 'Full P&L analysis, MoM and YoY comparisons, and a board-ready summary every month.',
    icon: 'DollarSign',
    prompt: 'Analyse revenue changes month over month, identify top drivers, and produce a board-ready summary.',
    triggers: ['cron'],
    outputs: ['email', 'pdf'],
    definition: {
      name: 'Monthly Revenue Summary',
      category: 'finance_executive',
      trigger: { type: 'cron', cron_expression: '0 9 1 * *', timezone: 'America/New_York' },
      steps: [
        {
          step_id: 'ingest_financials',
          agent_role: 'data_ingestor',
          depends_on: [],
          instructions: 'Pull 13 months of revenue data from Stripe including MRR, ARR, new/expansion/churn breakdown.',
          data_sources: [{ type: 'api', connector: 'stripe' }],
          input_sources: [],
          output_keys: ['financial_data'],
          timeout_ms: 45000,
        },
        {
          step_id: 'analyze',
          agent_role: 'analyst',
          depends_on: ['ingest_financials'],
          instructions: 'Compute MoM and YoY growth rates, identify revenue cohorts, flag anomalies, write board summary.',
          input_sources: ['financial_data'],
          output_keys: ['revenue_report'],
          timeout_ms: 90000,
        },
        {
          step_id: 'eval',
          agent_role: 'eval',
          depends_on: ['analyze'],
          instructions: 'Verify all figures are cited with exact numbers. Score accuracy and board-readiness.',
          input_sources: ['revenue_report'],
          output_keys: ['eval_result'],
          max_retries: 1,
          timeout_ms: 30000,
        },
        {
          step_id: 'deliver',
          agent_role: 'delivery',
          depends_on: ['eval'],
          instructions: 'Send as formatted email + attach PDF to configured recipients.',
          input_sources: ['revenue_report', 'eval_result'],
          output_keys: ['delivery_receipt'],
          timeout_ms: 15000,
        },
      ],
      output: { channels: [{ type: 'email', to: [] }, { type: 'report', format: 'pdf' }] },
    },
  },
  {
    id: 'churn-risk-monitor',
    name: 'Churn Risk Monitor',
    category: 'customer_success',
    description: 'Daily scan of CRM signals to surface accounts at risk before they cancel.',
    icon: 'AlertCircle',
    prompt: 'Identify accounts showing disengagement signals and recommend specific CS interventions.',
    triggers: ['cron'],
    outputs: ['email', 'webhook'],
    definition: {
      name: 'Churn Risk Monitor',
      category: 'customer_success',
      trigger: { type: 'cron', cron_expression: '0 7 * * MON-FRI', timezone: 'America/New_York' },
      steps: [
        {
          step_id: 'ingest',
          agent_role: 'data_ingestor',
          depends_on: [],
          instructions: 'Pull all CRM contacts with last_activity > 14 days, support tickets > 2, or NPS < 7.',
          data_sources: [{ type: 'api', connector: 'hubspot' }],
          input_sources: [],
          output_keys: ['crm_data'],
          timeout_ms: 30000,
        },
        {
          step_id: 'analyze',
          agent_role: 'analyst',
          depends_on: ['ingest'],
          instructions: 'Score each at-risk account (0–1 churn probability). Rank by MRR × risk. Recommend one action per account.',
          input_sources: ['crm_data'],
          output_keys: ['risk_report'],
          timeout_ms: 60000,
          consensus: { agent_count: 3, agreement_threshold: 0.67, reconciliation: 'highest_confidence' },
        },
        {
          step_id: 'deliver',
          agent_role: 'delivery',
          depends_on: ['analyze'],
          instructions: 'Send daily CS digest to configured Slack webhook and email.',
          input_sources: ['risk_report'],
          output_keys: ['delivery_receipt'],
          timeout_ms: 15000,
        },
      ],
      output: { channels: [{ type: 'email', to: [] }, { type: 'webhook', url: '' }] },
    },
  },
  {
    id: 'competitive-intel',
    name: 'Competitive Intelligence Tracker',
    category: 'product',
    description: 'Scrape competitor pricing pages and blogs weekly. Alert on changes that overlap your roadmap.',
    icon: 'Search',
    prompt: 'Monitor competitor websites for pricing changes, new features, and strategic announcements.',
    triggers: ['cron'],
    outputs: ['email', 'webhook'],
    definition: {
      name: 'Competitive Intelligence Tracker',
      category: 'product',
      trigger: { type: 'cron', cron_expression: '0 8 * * MON', timezone: 'America/New_York' },
      steps: [
        {
          step_id: 'scrape',
          agent_role: 'data_ingestor',
          depends_on: [],
          instructions: 'Scrape configured competitor URLs. Extract pricing, feature lists, and blog posts from the last 7 days.',
          data_sources: [{ type: 'web_scrape', url: '' }],
          input_sources: [],
          output_keys: ['competitor_data'],
          timeout_ms: 60000,
        },
        {
          step_id: 'analyze',
          agent_role: 'analyst',
          depends_on: ['scrape'],
          instructions: 'Identify pricing changes, new feature announcements, and strategic shifts. Rate threat level: low/medium/high.',
          input_sources: ['competitor_data'],
          output_keys: ['intel_report'],
          timeout_ms: 60000,
        },
        {
          step_id: 'deliver',
          agent_role: 'delivery',
          depends_on: ['analyze'],
          instructions: 'Send competitive intel digest to product team.',
          input_sources: ['intel_report'],
          output_keys: ['delivery_receipt'],
          timeout_ms: 15000,
        },
      ],
      output: { channels: [{ type: 'email', to: [] }] },
    },
  },
  {
    id: 'sales-pipeline-review',
    name: 'Sales Pipeline Review',
    category: 'sales',
    description: 'Weekly deal hygiene check — stale opportunities, missing next steps, and forecast roll-up.',
    icon: 'Target',
    prompt: 'Audit the sales pipeline for stale deals, flag missing next steps, and generate a weekly forecast.',
    triggers: ['cron'],
    outputs: ['email'],
    definition: {
      name: 'Sales Pipeline Review',
      category: 'sales',
      trigger: { type: 'cron', cron_expression: '0 8 * * FRI', timezone: 'America/New_York' },
      steps: [
        {
          step_id: 'ingest',
          agent_role: 'data_ingestor',
          depends_on: [],
          instructions: 'Pull all open deals from CRM. Flag deals with last_activity > 7 days or missing close date.',
          data_sources: [{ type: 'api', connector: 'hubspot' }],
          input_sources: [],
          output_keys: ['pipeline_data'],
          timeout_ms: 30000,
        },
        {
          step_id: 'analyze',
          agent_role: 'analyst',
          depends_on: ['ingest'],
          instructions: 'Compute weighted pipeline value, identify top 5 deals at risk, write forecast summary for sales manager.',
          input_sources: ['pipeline_data'],
          output_keys: ['pipeline_report'],
          timeout_ms: 60000,
        },
        {
          step_id: 'deliver',
          agent_role: 'delivery',
          depends_on: ['analyze'],
          instructions: 'Send pipeline review to sales manager.',
          input_sources: ['pipeline_report'],
          output_keys: ['delivery_receipt'],
          timeout_ms: 15000,
        },
      ],
      output: { channels: [{ type: 'email', to: [] }] },
    },
  },
  {
    id: 'infra-alert',
    name: 'Infrastructure Alert Monitor',
    category: 'operations',
    description: 'Receives webhook alerts from Datadog/PagerDuty, triages severity, auto-resolves P3/P4, and escalates P1/P2 to on-call for human approval before taking action.',
    icon: 'Server',
    prompt: 'Parse the incoming alert, assess severity, and decide whether to auto-resolve or escalate to a human.',
    triggers: ['webhook'],
    outputs: ['webhook', 'email'],
    definition: {
      name: 'Infrastructure Alert Monitor',
      category: 'operations',
      trigger: { type: 'webhook' },
      steps: [
        {
          step_id: 'parse_alert',
          agent_role: 'data_ingestor',
          depends_on: [],
          instructions: 'Parse the incoming webhook payload. Extract: service name, error message, affected region, metric values, alert source (Datadog/PagerDuty/CloudWatch). Output: { service, error_message, region, metrics, source, raw_payload }',
          input_sources: [],
          output_keys: ['alert_data'],
          timeout_ms: 15000,
        },
        {
          step_id: 'triage',
          agent_role: 'analyst',
          depends_on: ['parse_alert'],
          instructions: 'Assess alert severity. Map to P1 (service down, revenue impact), P2 (degraded, SLA risk), P3 (warning, no immediate impact), P4 (informational). Check runbook patterns. Output: { severity_level, summary, root_cause_hypothesis, recommended_action, auto_resolvable, confidence }',
          input_sources: ['alert_data'],
          output_keys: ['triage_result'],
          timeout_ms: 45000,
          consensus: { agent_count: 3, agreement_threshold: 0.67, reconciliation: 'highest_confidence' },
        },
        {
          step_id: 'escalate',
          agent_role: 'escalator',
          depends_on: ['triage'],
          instructions: 'Escalate this alert to the on-call engineer for human approval. Present the triage summary and recommended action. Wait for approval before proceeding.',
          input_sources: ['triage_result'],
          output_keys: ['escalation_result'],
          timeout_ms: 120000,
        },
        {
          step_id: 'resolve',
          agent_role: 'delivery',
          depends_on: ['escalate'],
          instructions: 'Send incident resolution notification. If approved: post to #incidents Slack channel with summary and actions taken. If rejected or timed out: page the senior engineer and mark as manually handled.',
          input_sources: ['triage_result', 'escalation_result'],
          output_keys: ['resolution_receipt'],
          timeout_ms: 15000,
        },
      ],
      output: { channels: [{ type: 'webhook', url: '' }, { type: 'email', to: [] }] },
    },
  },
  {
    id: 'custom',
    name: 'Custom Workflow',
    category: 'custom',
    description: 'Start from scratch. Define your own data sources, agents, and delivery channels.',
    icon: 'Wand2',
    prompt: '',
    triggers: ['manual', 'cron', 'webhook'],
    outputs: ['email', 'webhook', 'pdf'],
    definition: {
      name: 'Custom Workflow',
      category: 'custom',
      trigger: { type: 'manual' },
      steps: [],
      output: { channels: [] },
    },
  },
]

export function getTemplateById(id: string) {
  return WORKFLOW_TEMPLATES.find(t => t.id === id)
}

export const TEMPLATE_CATEGORIES = [
  { value: 'all', label: 'All' },
  { value: 'finance_executive', label: 'Finance' },
  { value: 'customer_success', label: 'Customer Success' },
  { value: 'sales', label: 'Sales' },
  { value: 'marketing', label: 'Marketing' },
  { value: 'operations', label: 'Operations' },
  { value: 'product', label: 'Product' },
  { value: 'custom', label: 'Custom' },
] as const
